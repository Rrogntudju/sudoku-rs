[package]
name = "sudoku"
version = "0.2.0"
edition = "2018"

[dependencies]
ahash = "0.7"
lazy_static = "1.4"
rand = { version = "0.8", optional = true }

[features]
test = ["rand"]

[profile.release]
opt-level = 3
lto = "fat" 
codegen-units = 1
